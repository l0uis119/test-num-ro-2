/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // three.js ships ESM that Next can bundle directly; nothing extra needed here.
};

export default nextConfig;
